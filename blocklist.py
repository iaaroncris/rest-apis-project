# from models import BlocklistModel
# from db import db

BLOCKLIST = set()
# new_list = list(BLOCKLIST)
# db.session.add_all(new_list)
# db.session.commit()

# result = db.session.query(BlocklistModel.token).all()
# result = [token for token, in result]
# BLOCKLIST = set(result)